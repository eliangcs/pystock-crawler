language: python
python:
  - 2.7
  - 3.2
  - 3.3
  - pypy
install:
  - pip install -r requirements.txt --use-mirrors
  - pip install pytest pytest-cov requests --use-mirrors
script:
  - py.test
after_success:
  - pip install python-coveralls --use-mirrors
  - coveralls
